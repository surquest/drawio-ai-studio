Create a detailed flowchart for a user authentication process using Google OAuth. 
Include the following steps:
1. User clicks "Login with Google".
2. Application redirects to Google Identity Services.
3. User enters credentials and authorizes the app.
4. Google redirects back to the app with an auth code.
5. App exchanges code for an access token.
6. App fetches user profile and establishes a session.
7. User is redirected to the dashboard.

Add clear decision diamonds for "Success?" and "User canceled?".
Use standard flowchart shapes.